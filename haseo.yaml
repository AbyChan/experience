name: experience
flow:
  - install: git submodule init && git submodule update && yarn install
  - yarn theme: cd theme && yarn install
  - build: ./node_modules/.bin/holothuroidea . ./theme/static/data
  - render: cd theme && npm run generate
